<table>
    <tr>
        <th>ISBNコード</th><th>書名</th><th>価格</th>
        <th>出版社</th><th>刊行日</th><th>ダウンロード</th>
    </tr>
    <!--
    <% @books.each do |book| %>
        <tr style="background-color: <%= cycle('#FFffFF', '#FFff99', '#FF8800') %>">
            <td><%= book.isbn %></td>
            <td><%= book.title %></td>
            <td><%= book.price %>円</td>
            <td><%= book.publish %></td>
            <td><%= book.published %></td>
            <td><%= book.dl %></td>
        </tr>
    <% end %>
    -->
    <% @books.each do |book| %>
        <tr>
            <td style="background-color: <%= cycle('#FFffFF', '#FFff99', name: 'yellow') %>">
                <%= book.isbn %>
            </td>
            <td style="background-color: <%= current_cycle(name = 'yellow') %>">
                <%= book.title %>
            </td>
            <td style="background-color: <%= cycle('#FFffFF', '#FFCCE5', name: 'pink') %>">
                <%= book.price %>円
            </td>
            <td style="background-color: <%= current_cycle(cname = 'blue') %>">
                <%= book.publish %>
            </td>
            <td style="background-color: <%= cycle('#FFffFF', '#B0EDFF', name: 'blue') %>">
                <%= book.published %>
            </td>
            <td style="background-color: <%= current_cycle(cname = 'pink') %>">
                <%= book.dl %>
            </td>
        </tr>
    <% end %>
</table>